[package]
name = "LM"
version = "0.1.0"
edition = "2024"

[dependencies]

# The "Engine" for the terminal UI
ratatui = "0.26"
# Handles keyboard inputs and terminal control
crossterm = "0.27"
# Helps us manage errors without the app crashing
anyhow = "1.0"
